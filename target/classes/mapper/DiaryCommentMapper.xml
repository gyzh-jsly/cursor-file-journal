<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.filejournal.mapper.DiaryCommentMapper">

    <insert id="insert" parameterType="com.filejournal.model.DiaryComment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO diary_comment (diary_id, version, selected_text, start_offset, comment, created_at)
        VALUES (#{diaryId}, #{version}, #{selectedText}, #{startOffset}, #{comment}, #{createdAt})
    </insert>

    <select id="selectByDiaryId" resultType="com.filejournal.model.DiaryComment">
        SELECT * FROM diary_comment
        WHERE diary_id = #{diaryId}
        ORDER BY start_offset, created_at
    </select>
</mapper>